plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
    id 'org.spongepowered.mixin' version '0.7.+'
//    id 'org.parchmentmc.librarian.forgegradle' version '1.+'
}

version = mod_version
group = mod_group_id

base {
    archivesName = mod_name
}

// Mojang ships Java 17 to end users in 1.18+, so your mod should target Java 17.
java.toolchain.languageVersion = JavaLanguageVersion.of(17)

println "Java: ${System.getProperty 'java.version'}, JVM: ${System.getProperty 'java.vm.version'} (${System.getProperty 'java.vendor'}), Arch: ${System.getProperty 'os.arch'}"
minecraft {
    mappings channel: mapping_channel, version: mapping_version
    copyIdeResources = true
    accessTransformer = file('src/main/resources/META-INF/accesstransformer.cfg')
    runs {
        // applies to all the run configs below
        configureEach {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'info'
            property 'mixin.env.remapRefMap','true'
            property 'mixin.env.refMapRemappingFile', "${project.projectDir}/build/createSrgToMcp/output.srg"
            arg "-mixin.config=endless.mixins.json"
            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }

        client {
            // Comma-separated list of namespaces to load gametests from. Empty = all namespaces.
            property 'forge.enabledGameTestNamespaces', mod_id
        }

        server {
            property 'forge.enabledGameTestNamespaces', mod_id
            args '--nogui'
        }
        gameTestServer {
            property 'forge.enabledGameTestNamespaces', mod_id
        }

        data {
            // example of overriding the workingDirectory set in configureEach above
            workingDirectory project.file('run-data')

            // Specify the modid for data generation, where to output the resulting resource, and where to look for existing resources.
            args '--mod', mod_id, '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

repositories {
    maven { url 'https://maven.aliyun.com/repository/public/' }
    maven { url 'https://maven.aliyun.com/repository/gradle-plugin/' }
    maven { url 'https://maven.aliyun.com/repository/forge/' }
    maven { url 'https://maven.aliyun.com/repository/central/' }

    maven {
        url "https://www.cursemaven.com"
        content {
            includeGroup "curse.maven"
        }
    }
    maven { url 'https://maven.blamejared.com' }
    maven {
        name = "Curios"
        url = uri("https://maven.theillusivec4.top/")
    }
    maven {
        name = 'Favouriteless Maven'
        url 'https://maven.favouriteless.net/releases'
    }
    // flatDir {
    //     dir 'libs'
    // }
}

dependencies {
    minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"

    compileOnly fg.deobf("vazkii.botania:Botania:${minecraft_version}-${bot_ver}")
//    compileOnly fg.deobf("curse.maven:re-avaritia-623969:${ra_id}")
    compileOnly fg.deobf("curse.maven:blood-magic-224791:${bloodmagic_id}")
    compileOnly fg.deobf("curse.maven:enchanted-witchcraft-560363:${enchanted_id}")
    compileOnly fg.deobf("curse.maven:draconic-evolution-223565:${de_id}")
    compileOnly fg.deobf("curse.maven:curios-309927:${curios_id}")
    compileOnly fg.deobf("curse.maven:jade-324717:${jade_id}")
    implementation fg.deobf("mezz.jei:jei-${minecraft_version}-forge:${jei_version}")
    compileOnly fg.deobf("curse.maven:crafttweaker-239197:${crt_id}")
//    implementation fg.deobf("curse.maven:codechicken-lib-1-8-242818:5753868")

    runtimeOnly fg.deobf("curse.maven:jade-324717:${jade_id}")
//    runtimeOnly fg.deobf("curse.maven:re-avaritia-623969:${ra_id}")
    runtimeOnly fg.deobf("vazkii.patchouli:Patchouli:${minecraft_version}-${patchouli_ver}")
//    runtimeOnly fg.deobf("vazkii.botania:Botania:${minecraft_version}-${bot_ver}")
//    runtimeOnly fg.deobf("curse.maven:blood-magic-224791:${bloodmagic_id}")
//    runtimeOnly fg.deobf("curse.maven:enchanted-witchcraft-560363:${enchanted_id}")
//    runtimeOnly fg.deobf("curse.maven:draconic-evolution-223565:${de_id}")
    runtimeOnly fg.deobf("curse.maven:curios-309927:${curios_id}")
    runtimeOnly fg.deobf("curse.maven:crafttweaker-239197:${crt_id}")
    runtimeOnly fg.deobf("curse.maven:codechicken-lib-1-8-242818:${ccl_id}")
    runtimeOnly fg.deobf("curse.maven:brandons-core-231382:${bc_id}")
    runtimeOnly fg.deobf("curse.maven:draconic-evolution-223565:${de_id}")
    runtimeOnly fg.deobf("curse.maven:selene-499980:4806893")
    runtimeOnly fg.deobf("curse.maven:mmmmmmmmmmmm-225738:${mmmm_id}")

    annotationProcessor "org.spongepowered:mixin:${mixin_version}:processor"
}

tasks.named('processResources', ProcessResources).configure {
    var replaceProperties = [
            minecraft_version: minecraft_version, minecraft_version_range: minecraft_version_range,
            forge_version: forge_version, forge_version_range: forge_version_range,
            loader_version_range: loader_version_range,
            mod_id: mod_id, mod_name: mod_name, mod_license: mod_license, mod_version: mod_version,
            mod_authors: mod_authors, mod_description: mod_description,
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml', 'pack.mcmeta']) {
        expand replaceProperties + [project: project]
    }
}

// Example for how to get properties into the manifest for reading at runtime.
tasks.named('jar', Jar).configure {
    manifest {
        attributes([
                'Specification-Title'     : mod_id,
                'Specification-Vendor'    : mod_authors,
                'Specification-Version'   : '1', // We are version 1 of ourselves
                'Implementation-Title'    : project.name,
                'Implementation-Version'  : project.jar.archiveVersion,
                'Implementation-Vendor'   : mod_authors,
                'Implementation-Timestamp': new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }

    // This is the preferred method to reobfuscate your jar file
    finalizedBy 'reobfJar'
}

mixin {
    //指定refmap.json文件生成位置
    add sourceSets.main, "endless.refmap.json"
    config "endless.mixins.json"
}

publishing {
    publications {
        register('mavenJava', MavenPublication) {
            artifact jar
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/mcmodsrepo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
}
